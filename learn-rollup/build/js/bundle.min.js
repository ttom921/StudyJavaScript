var bundle = (function () {
    'use strict';

    var mod1 = 'hello world!';

    // Import styles (automatically injected into <head>).
    function main () {
        console.log(mod1);
    }

    return main;

}());
